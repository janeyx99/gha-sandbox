on: pull_request
jobs:
  big:
    runs-on: ubuntu-20.04
    steps:
      - uses: technote-space/auto-cancel-redundant-workflow@v1
      - run: |
          echo 'about to wait'
          sleep 150
          echo 'eating potatoes in the meantime'
          sleep 50
          echo 'done waiting'
# comment
